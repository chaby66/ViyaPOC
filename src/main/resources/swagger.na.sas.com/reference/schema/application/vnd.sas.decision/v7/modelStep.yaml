id: swagger.na.sas.com/reference/schema/application/vnd.sas.decision/v7/modelStep.yaml
title: Model Decision Step
description: Represents a model be referenced as a step within a decision flow.
required:
    - name
    - id
properties:
    name:
        type: string
        description: Name of the model being referenced.
    id:
        type: string
        description: 'Id of the model that is being referenced.'